[project]
name = "page-vault"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "beautifulsoup4>=4.14.3",
    "fastapi>=0.128.0",
    "uvicorn>=0.40.0",
]

[tool.pyright]
venvPath = "."
venv = ".venv"
